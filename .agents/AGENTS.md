# Agent Rules

## Token Usage
- Use minimal tokens. Be concise in all responses.
- Do not narrate actions while working (e.g. 'I will now do X').
- Only summarize what changed at the very end, briefly.
- Avoid verbose explanations — short bullet points only.
